name: 'Auto Assign PRs'
on: pull_request

jobs:
  add-reviewers:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@v1.1.1
        with:
          configuration-path: ".github/pr-reviewers.yml" # Only needed if you use something other than .github/auto_assign.yml
